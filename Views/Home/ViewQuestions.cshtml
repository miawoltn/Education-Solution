@model IEnumerable<Education_Solution.Models.Question>

@{
    ViewBag.Title = "ViewQuestions";
}

<h2>ViewQuestions</h2>

<p>
    @Html.ActionLink("Create New", "PostQuestion")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
           @* @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |*@
            @Html.ActionLink("Details", "GetQuestion", new { id=item.Id }) |
            @Html.ActionLink("Delete", "DeleteQuestion", new { id=item.Id })
        </td>
    </tr>
}

</table>
